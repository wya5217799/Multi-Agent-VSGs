#!/bin/bash
# Stub: future GPU SAC backend (per spec section 11). Currently REJECTED (network 256 too small).
# Kept so SKILL.md / daemon route table does not error on missing path.
echo "sac_gpu backend not implemented (spec section 11.5: GPU REJECT, hidden >=1024 needed)" >&2
exit 99
