[pytest]
testpaths = tests
norecursedirs = tests/archive
markers =
    slow: requires model loading or long simulation
    smoke_simulink: online integration test requiring MATLAB engine (~30s)
    typeA: probe_state Type A invariant — data-independent (paper FACT / contract)
    typeB: probe_state Type B invariant — phase-data-required (fail-soft skip)
